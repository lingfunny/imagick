#include <iostream>

#include <opencv2/core.hpp>

#include "ImageLoader.hpp"

int main(int argc, char** argv) {
    std::cout << "Imagick CLI 初始化完成，后续功能将逐步添加。" << std::endl;
    if (argc > 1) {
        std::cout << "当前暂未实现命令解析，请等待后续模块。" << std::endl;
    }
    return 0;
}
